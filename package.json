{
  "name": "fullstack_boilerplate_mrfn",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "server-start": "npm start --prefix backend",
    "server-dev": "npm dev --prefix backend",
    "server-test": "npm test --prefix backend",
    "client": "npm dev --prefix frontend",
    "dev": "concurrently \"npm run server-dev\" \"npm run client\"",
    "build": "npm build --prefix frontend",
    "install": "npm install --prefix backend && npm install --prefix frontend"
  },
  "author": "Rinaldo Benaccetta",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^7.3.0"
  }
}
