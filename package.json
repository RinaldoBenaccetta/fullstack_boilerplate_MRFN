{
  "name": "fullstack_boilerplate_mrfn",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "server-start": "npm start --prefix backend",
    "server-dev": "npm run dev --prefix backend",
    "server-test": "npm test --prefix backend",
    "client-dev": "npm run dev --prefix frontend",
    "client-preview": "npm run preview --prefix frontend",
    "dev": "npm run client-dev & npm run server-dev",
    "build": "npm run build --prefix frontend",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "author": "Rinaldo Benaccetta",
  "license": "MIT",
  "dependencies": {
  },
  "devDependencies": {
    "concurrently": "^7.3.0"
  }
}
